**Switch** — binary toggle for settings (auto-update, notifications). Track turns teal when on; restrained throw, no bounce.

```jsx
<Switch label="Auto-update modules" defaultChecked />
<Switch label="Pause activations" />
```

Standard input props pass through (`checked`, `onChange`, `disabled`).
