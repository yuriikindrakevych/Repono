**Checkbox** — square checkbox, teal fill when checked, with optional description line.

```jsx
<Checkbox label="Auto-update on new releases" defaultChecked />
<Checkbox label="Email me on failed activations" description="Sent to the account owner only" />
```

Standard input props pass through (`checked`, `defaultChecked`, `onChange`, `disabled`).
